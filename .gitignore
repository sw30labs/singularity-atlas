# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Tooling caches / coverage
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# Virtual environments
.venv

# Runtime state at the repo root. The committed snapshot is baseline/data/.
/data/
/data.bak-before-import-*/

# Local migrate tarballs. GitHub allows .tar.gz, but the tracked baseline is
# the unpacked neo4j.dump + JSON under baseline/ (well under the 100 MB cap).
/singularity-atlas-migrate-*.tar.gz
/baseline/*.tar.gz
/atlas-export/

# Third-party published works: kept locally, never published.
ref/innermost-loop/
ref/accelerando/
transcriptions_moonshot/
observatory-output/
